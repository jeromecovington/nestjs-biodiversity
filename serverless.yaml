service: nestjs-biodiversity

frameworkVersion: '3'

plugins:
  - serverless-offline

provider:
  name: aws
  runtime: nodejs20.x
  region: us-east-1

functions:
  graphql:
    handler: dist/lambda.handler
    events:
      - http:
          method: ANY
          path: graphql
